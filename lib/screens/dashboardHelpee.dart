import 'dart:core';
import 'package:flutter/material.dart';

class HelpeeDashboard extends StatelessWidget {
  const HelpeeDashboard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Helpee");
  }
}
